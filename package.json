{
  "name": "vm-proxy",
  "version": "1.0.0",
  "description": "Just a small proxy to redirect some ports in the VM",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-proxy": "^1.18.0"
  }
}
